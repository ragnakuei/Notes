# 第三方物件互動

```cs
[Test]
public async Task Test()
{
    // Arrange
    var test = Substitute.For<ITest>();
    var target = new TestRun(test);

    // Act
    target.Run();

    // Assert
    test.Received(1).Run();
}
```
