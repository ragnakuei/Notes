# 虛擬環境

主要用意是為了讓各 python 專案所執行的 python 版本、套件，能夠互相獨立而互不影響 !

## virtualenv

-   透過 poetry 建立專案後，預設就會套用了 !
-   暫時先不 Study 手動建立方式，因為要安裝額外的 cli

停用當前目錄所使用的 virtualenv 環境

```bash
deativate
```

啟用當前目錄所使用的 virtualenv 環境

```bash
source .venv/bin/activate
```
